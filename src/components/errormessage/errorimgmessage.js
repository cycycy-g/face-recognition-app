import React from 'react';

const ErrorImgMessage = () => (
  <div style={{fontSize: '1rem', color: 'red'}}>
    <p>Something went wrong. Please try again and enter a valid URL with Face.</p>
  </div>
);

export default ErrorImgMessage;